import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: 'FFCA6D',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });
  
  export {styles}